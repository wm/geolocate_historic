<div id="animations">
    <div class="toolbar">
        <h1>PlacesNearYou</h1>
        <a class="back" href="#">Home</a>
    </div>
    <ul class="rounded">
      <% @query_locations.each do |location| %>
        <li><%= location.ip %><br/><%= location.lat %>,<%= location.lng %></li>
      <% end %>
    </ul>
</div>
